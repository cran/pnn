#' hold-out method give an idea
hold.out <- function() {
    
}

function.to.minimize <- function(pars) {
    
}

optim.pnn <- function(nn) {
    #require(rgenoud)
    #range <- matrix(c(0,2), ncol=2, byrow=TRUE)
    #opt <- genoud(fn=, nvars=1, Domains=range, data.type.int=TRUE, boundary.enforcement=2, pop.size=1000)
    #nn$opt <- opt
    #nn$sigma <- opt$par[1]
    #return(nn)
}

#' Plot the performance of sigma for a systematic exploration
plot.sigma <- function() {
    
}